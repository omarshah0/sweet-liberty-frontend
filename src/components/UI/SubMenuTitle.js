import React from "react"

const SubMenuTitle = ({ title }) => {
  return (
    <h3 className="uppercase font-frankRuhlLibre text-xl font-bold text-brandRed mb-6">
      {title}
    </h3>
  )
}

export default SubMenuTitle
